;;; -*- lexical-binding: t -*-
(use-package doom-themes
  :ensure t)

(add-to-list 'custom-theme-load-path "~/.emacs.d/themes/")

;; Install package "gruvbox-theme" with "package-install".
(setq prelude-theme 'doom-xcode)
